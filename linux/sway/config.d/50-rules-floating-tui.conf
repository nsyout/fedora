# Float and center TUI apps launched from waybar/rofi
for_window [title="^bluetuith$"] {
    floating enable
    resize set width 800 height 500
    move position center
}

for_window [title="^wiremix$"] {
    floating enable
    resize set width 800 height 500
    move position center
}
