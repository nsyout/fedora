# Dynamic output configuration (display scaling, arrangement)
exec_always bash -c 'pkill kanshi; sleep 0.5; kanshi'

# Start notification daemon (required for volume/brightness OSD)
exec mako

# OpenSnitch UI (outbound firewall prompts)
exec opensnitch-ui
