# Base packages — always installed
# One package per line. Lines starting with # are comments.

# Shell & terminal
zsh
tmux
starship  # managed by update-tools.sh, listed for doc purposes

# Editors
neovim

# Search & navigation
ripgrep
fd-find
fzf
zoxide
bat
eza

# Git & VCS
git
gh
lazygit  # managed by update-tools.sh

# Build & dev tools
zig  # general development; Ghostty updater manages its exact required version
jq
just
ShellCheck
shfmt

# Biometric auth
fprintd

# AppImage support
fuse
fuse-libs

# System & security
fail2ban
chrony
openssh-clients
libfido2
gitleaks
socat
wget2
curl

# Power management
tlp
powertop

# Console font (HiDPI TTY)
terminus-fonts-console

# Utilities
trash-cli
prettyping
btop
fastfetch
ncdu
diff-so-fancy

# Encryption
age

# File managers
thunar
yazi  # managed by update-tools.sh

# Browser
firefox

# Networking
wpa_supplicant
NetworkManager-wifi
NetworkManager-tui

# Wayland desktop
kanshi
waybar
mako
libnotify
grim
slurp
wl-clipboard
brightnessctl
playerctl
pavucontrol
xdg-user-dirs
sway-systemd

# PolicyKit agent
lxqt-policykit

# Audio
pipewire-pulseaudio
pulseaudio-utils

# Firewall
opensnitch  # managed by update-tools.sh

# VPN
tailscale

# Launcher
rofi

# Display manager
greetd
tuigreet

# App infrastructure
flatpak
# Personal profile packages — installed on personal machines
# One package per line. Lines starting with # are comments.

# YubiKey
yubikey-manager

# Media
yt-dlp
ffmpeg-free
aria2

# Blog & dev
hugo
uv
rustup
golang
ansible

# Apps (1Password repo must be configured first)
1password
1password-cli

# Media
vlc

# Editors (Sublime Text repo must be configured first)
sublime-text

# Cloud storage — moved to flatpaks.personal

# Gallery downloads (managed by update-tools.sh via uv)
gallery-dl

# AI coding agent

# Chat
slack  # RPM from slack.com/downloads/linux
discord  # managed by update-tools.sh

# Cloud storage (Dropbox repo must be configured first)
dropbox
