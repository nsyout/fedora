# Global Preferences

## Task Execution

- When possible, break complex tasks into independent subtasks that can be executed in parallel
- Look for opportunities to run tests, linting, and builds concurrently with development tasks
- Explicitly identify which tasks have dependencies and which can run independently
