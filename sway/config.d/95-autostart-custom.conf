# Start notification daemon (required for volume/brightness OSD)
exec mako

# Keep clipboard alive after source app closes
exec wl-paste --watch wl-copy

# OpenSnitch UI (outbound firewall prompts)
exec opensnitch-ui
