# Screenshot keybindings
# Requires: grim, slurp

bindsym {
    # Select area
    $mod+Shift+s exec grim -g "$(slurp)" ~/pictures/screenshots/$(date +%Y%m%d-%H%M%S).png
    # Full screen
    $mod+Ctrl+s exec grim ~/pictures/screenshots/$(date +%Y%m%d-%H%M%S).png
}
